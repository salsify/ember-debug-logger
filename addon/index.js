import {
  overrideLoggers,
  debugLogger,
  infoLogger,
  warnLogger,
  errorLogger
} from 'ember-debug-logger/utils/debug-logger';

export {
  overrideLoggers,
  debugLogger,
  infoLogger,
  warnLogger,
  errorLogger
};

export default debugLogger;
